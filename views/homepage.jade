extends ./layout

block body
	p Name:
		input(type="text", ng-model="name")
	h1
		Hello {{name}}

	ul(ng-controller="MessageController")
		li(ng-repeat="message in messages")
			{{message.createdAt}} | {{message.content}}